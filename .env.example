# Optional host hints.
# Set only when the surrounding ORCA environment exposes a usable browser tool.
ORCA_BROWSER_AVAILABLE=0
ORCA_RUNTIME=0

# Optional future hook: a local module that returns a codex-chatgpt-control
# compatible BrowserLike object.
# CHATGPT_WEB_BRIDGE_BROWSER_MODULE=/absolute/path/provider.mjs
